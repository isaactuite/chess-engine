#include <stdio.h>
#include "defs.h"
#include "game_logic.h"


int evaluate(){
    int score();
    for (int i = 0; i<8; i++){
        for (int j = 0; j<8; j++){
            if (check_piece_color(bot_board[i][j])  == 0){
                
            }
        }
    }
}
int alphaBetaMax( int alpha, int beta, int depthleft )